# conda environment file for python 2, includes packages needed for testing
# create a conda virtual env using:
#   conda env create -n myenv --file py2.7_conda_environment.yml
#   source activate myenv (use 'activate myenv' on windows)
#   python setup.py develop
#
# optionally run tests:
#   python setup.py test

name: quest

channels:
    - conda-forge
    - erdc
    - defaults

dependencies:
    - python=2.7
    - futures
    - fiona
    - rasterio
    - gdal
    - numpy
    - geojson
    - jinja2
    - ulmo>=0.8.3.2
    - pyyaml
    - matplotlib
    - click
    - werkzeug
    - peewee
    - geopandas
    - distributed>=1.18
    - psutil
    - pint
    - pony
    - tornado
    - pytest-cov
    - coveralls
    - param
    - sphinx_rtd_theme
    - hs_restclient
    - jupyter
    - girder-client
    - terrapin
    - ncep_client