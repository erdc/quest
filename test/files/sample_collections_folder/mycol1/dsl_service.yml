dsl:
    file_type: service
    file_version: 1.0

metadata:
    display_name: Sample Local Service
    description: This service is for testing
    tags:

# features can be hdf5/geojson/csv
features:
    file: features.h5
    format: hdf5

parameters:
    precipitation:
        display_name: Precipitation
        description: blah blah
        features:
            map: data/<feature_id>.csv
            format: csv
            format_kwargs:
                column_name: 'values'